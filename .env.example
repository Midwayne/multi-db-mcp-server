# Optional. Referenced from spec.yaml via ${MONGO_URI} style placeholders.
MONGO_URI=mongodb://localhost:27017
POSTGRES_URI=postgres://postgres:postgres@localhost:5432/app?sslmode=disable
REDIS_URI=redis://localhost:6379/0

# Optional path to the spec file. Defaults to ./spec.yaml
# DBMCP_SPEC=./spec.yaml
