# GOPROXY configuration, used for Go module proxying
GOPROXY="https://proxy.golang.org,direct"

# Comma-separated list of tools to enable
TOOLS="find,aggregate,count,collection-indexes,collection-schema,db-stats"

# MongoDB connection URIs
# Add as many MONGO_CONN* variables as you need
MONGO_CONN_1="mongodb://localhost:27017"
MONGO_CONN_2="mongodb://another-host:27017"

SERVE_MODE="stdio" # Other options include sse and http (stremeable http)
PORT="8080" # Defaults to port 8080 if using http